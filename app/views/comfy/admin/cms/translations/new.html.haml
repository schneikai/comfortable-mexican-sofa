.page-header
  %h2= t('.title')

= comfy_form_for @translation, :as => :translation, :url => {:action => :create}, :html => {:multipart => true, :data => {:live_preview => true}} do |form|
  = render form
